# Hippocampus 部署指南

想让更多人使用你的“海马体”刷题系统？这里有两个方案：

## 方案一：局域网极速共享（推荐 🔥）
**场景**：可以给同一个办公室、教室或宿舍的朋友使用（需要连接同一个 Wi-Fi）。
**成本**：0元，无需购买服务器。

### 步骤：
1. **获取本机 IP**
   - 打开一个新的 CMD 终端。
   - 输入命令 `ipconfig` 并回车。
   - 找到 **“无线局域网适配器 WLAN”** 下的 `IPv4 地址`。
   - 假设它是：`192.168.1.5`（具体以你看到的为准）。

2. **启动服务**
   - 回到正在运行项目的终端（确保没有报错）。
   - 按 `Ctrl + C` 停止当前服务。
   - 输入以下命令启动：
     ```bash
     python manage.py runserver 0.0.0.0:8000
     ```
   - *（注意：必须加 `0.0.0.0`，否则别人连不上）*

3. **分享链接**
   - 把这个链接发给朋友：`http://192.168.4.233:8000` (把 IP 换成你查到的那个)。
   - 他们用手机或电脑浏览器打开，就可以开始刷题了！

---

## 方案二：云服务器部署（专业版）
**场景**：想让互联网上任何人都能访问（比如发给异地的朋友）。
**成本**：需要购买一台云服务器（阿里云/腾讯云等），约 50-100元/月。

### 步骤概要：
1. 购买一台 **Ubuntu 22.04** 服务器。
2. 将代码上传到服务器。
3. 如果是首次部署，在服务器上安装 Python 依赖：
   ```bash
   pip install -r requirements.txt
   ```
4. 构建并初始化数据库：
   ```bash
   python manage.py migrate
   ```
5. 使用专业 Web 服务器启动（不要用 runserver）：
   ```bash
   gunicorn hippocampus_project.wsgi --bind 0.0.0.0:80
   ```
6. 直接访问服务器的公网 IP 即可。

> 注意：云服务器记得在防火墙/安全组开放 **80** 端口。

## 方案三：上传代码到 GitHub (代码备份/协作)
**场景**：保存代码，或者与他人分享代码。这不是直接“运行”网站，而是“存储”代码。

### 步骤：
1. **初始化仓库** (在终端执行)
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```

2. **创建 GitHub 仓库**
   - 登录 GitHub，点击右上角 `+` -> `New repository`。
   - 输入仓库名（例如 `Hippocampus`），点击 `Create repository`。

3. **推送到 GitHub**
   - 复制 GitHub 页面上显示的 **"…or push an existing repository from the command line"** 下面的几行命令。
   - 通常是：
     ```bash
     git remote add origin https://github.com/你的用户名/Hippocampus.git
     git branch -M main
     git push -u origin main
     ```

## 方案四：使用 Render 免费部署 (推荐给外部用户访问) 🌐
**场景**：GitHub 本身只能托管代码（像网盘），不能直接运行 Python/Django 网站。要让大家都能访问，需要配合 **Render** 这样的平台（只要有 GitHub 账号即可免费使用）。

### 步骤：
1. **完成方案三**：确保你的代码已经推送到 GitHub。
2. **注册 Render**：
   - 访问 [render.com](https://render.com/)，点击 "Get Started"，选择用 GitHub 账号登录。
3. **创建 Web Service**：
   - 点击 "New +" -> "Web Service"。
   - 在列表中选择你的 `Hippocampus` 仓库，点击 "Connect"。
4. **配置参数**：
   - **Name**: 起个名字 (比如 `hippocampus-app`)。
   - **Region**: 默认即可 (Singapore 或 Oregon)。
   - **Branch**: `main`
   - **Runtime**: `Python 3`
   - **Build Command**: `./build.sh` (我已经帮你写好了这个脚本)
   - **Start Command**: `gunicorn hippocampus_project.wsgi:application`
   - **Instance Type**: 选择 "Free" (免费版)。
5. **点击部署**：
   - 点击 "Create Web Service"。
   - 等待几分钟，部署完成后，Render 会给你一个网址（例如 `https://hippocampus-app.onrender.com`）。
   - 把这个网址发给任何人，他们都能访问了！
